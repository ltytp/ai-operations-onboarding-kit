# 導入前診断から運用保守Repositoryへの引継ぎ

実行手順と受入条件は`bootstrap/README.md`を参照してください。

このRepositoryは診断対象の原本をCopyしません。承認済みOnboarding Package、現行参照用Inventory、初期Graph seed、Import receiptだけをSystem ID単位で保持します。これにより、原本の所在、診断時点、承認者、導入後のGraphが追跡できます。
